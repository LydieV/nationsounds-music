{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <div class="container">
        <h1>Artiste</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ artiste.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ artiste.name }}</td>
                </tr>
                <tr>
                    <th>Style</th>
                    <td>{{ artiste.style }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('artiste_index') }}" class="badge badge-primary text-uppercase">Retour à la liste</a>

        <a href="{{ path('artiste_edit', {'id': artiste.id}) }}" class="badge badge-primary text-uppercase">Modifier</a>

        {{ include('artiste/_delete_form.html.twig') }}
    </div>
{% endblock %}
