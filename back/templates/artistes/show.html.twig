{% extends 'base.html.twig' %}

{% block title %}artistes{% endblock %}

{% block body %}

    <div class="container">
        <h1 class="text-center">artistes</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ artistes.id }}</td>
                </tr>
                <tr>
                    <th>Titre</th>
                    <td>{{ artistes.name }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('artistes_index') }}"  class="badge badge-primary">Retour à la liste</a>

        <a href="{{ path('artistes_edit', {'id': artistes.id}) }}"  class="badge badge-primary">Modifier</a>

        {{ include('artistes/_delete_form.html.twig') }}
    </div>
{% endblock %}